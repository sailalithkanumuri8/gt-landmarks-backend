# Docker Compose — GT Landmarks (App + MongoDB)
# Start:  docker compose up --build
# Stop:   docker compose down

services:
  # WORKSHOP TODO: Define the MongoDB service (3 minutes)



  # WORKSHOP TODO: Define the Flask app service (5 minutes)



# WORKSHOP TODO: Declare the named volume (1 minute)

